#include "main.h"
#include <stdio.h>
/**
 * main - swap the value of twoo integers
 * @a: value to be swap
 * @b: value to be swap
 *
 * Return: always 0
 */

void swap_int(int *a, int *b);
{
	int *a;
	int *b;

	*a = 98;
	*b = 42;
	_putchar("value of '*a' , : %d\n" && "value of '*b' , : %d\n", a, b,);
	_putchar("swap_int '*a & *b, : %d\n")
	_putchar("value of '*a' , : %d\n" && "value of '*b' , : %d\n", a, b,);
	return (0);
}
