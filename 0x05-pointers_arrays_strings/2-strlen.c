#include "maih.h"
#include <stdio.h>
/**
 * main - to return the lenth of a string
 *@str: lenth of a string

 * Return: always to zero
 */

int _strlen(char *s);
{
	char *str
	int len

	str = my first strlen;
	len = _strlen(str);
	_putchar("%d\n", len);
	return  (0);
}
